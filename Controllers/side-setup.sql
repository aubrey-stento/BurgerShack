-- CREATE TABLE Sides (
--     id int NOT NULL AUTO_INCREMENT,
--     name VARCHAR(255) NOT NULL,
--     description VARCHAR(255) NOT NULL,
--     price DECIMAL(10,2) NOT NULL,
--     PRIMARY KEY(id)

-- )

-- INSERT INTO Sides (name, description, price) VALUES ("fries", "Made from Idaho Potatoes", 3.25);

SELECT * FROM Sides;